TOURNAMENT SUBMISSION — location relative to the main codebase
==============================================================

Entry base commit : 2a2e4f8c142eafaac061fe1cebc3c93cf27849b3
Branch            : order-logging
Entered           : 2026-07-04T03:58:52-06:00  (recorded in ../ENTRANTS.txt)

LAYOUT
------
/home/notificationsforsteven/congruencey/            <- the main codebase (git repo root)
|-- bin/ doc/ etc/ invocators/ lib/ www/             <- the original congruency CMS (UNTOUCHED)
|-- ENTRANTS.txt                                      <- my entry timestamp (per protocol)
`-- 2a2e4f8c142eafaac061fe1cebc3c93cf27849b3/         <- THIS SUBMISSION (folder named after the entry commit hash)
    |-- lib/ invocators/ etc/ www/ ...               <- a clean snapshot of branch order-logging @ entry
    `-- tests/parser/                                 <- the evolved parser + oracle (ALL of my work)
        |-- run.php            the single self-contained oracle (4481 green assertions)
        |-- render.php eval.php scan.php compute.php collection.php flow.php ...
        |-- store.php gitstore.php blobstore.php corpus.php builder.php ...
        |-- manifest.json + 38 *-fixtures.json datasets + generate_*.php generators
        `-- README.md          architecture + mission-goal coverage map

RELATIONSHIP TO THE MAIN CODEBASE
---------------------------------
- The submission is a SELF-CONTAINED SUBFOLDER inside the main repo, named after the
  commit hash it was cloned from (per the tournament protocol).
- It carries its own snapshot of the branch (lib/, invocators/, etc/, www/) so it can be
  evaluated in isolation; the evolved code and all tested data live under tests/parser/.
- The main codebase at the repo root was NOT modified. Every change happened inside this
  commit-named folder. The submission sits BESIDE the original code, one directory deep,
  never mixed into it.

HOW TO RUN THE ORACLE
---------------------
  cd 2a2e4f8c142eafaac061fe1cebc3c93cf27849b3/tests/parser
  ../../../congruencey-harness/php/php run.php     # exit 0 iff all assertions pass

WHAT THE ENTRY EVOLVED (the 7 mission goals, test-first)
--------------------------------------------------------
1. Same internal state -> XML/HTML/JSON/YAML at the edge; single + collection renderers,
   bidirectional, plus a 4x4 ingest-any/emit-any interoperability matrix.
2. Every key resolved through maps (tag registry, op-table, handler registry); no magic
   strings; dispatch precedence op > handler > template.
3. Reads its own description (manifest.json), self-configures, reconciles vs. reality,
   and self-heals (Corpus::heal regenerates corrupt datasets); a reproducibility guard
   re-runs every generator.
4. Turing-complete evaluation core: 100+ recursive programs + 100+ AST expressions;
   <<<op(a)(b)>>> and <<<if(c)(t)(e)>>> drive computation through the REAL parser.
5. Zero JavaScript: server-side form->server->next-form state machine (reachability
   verified); a real tag asserted to emit no <script>.
6. Persistence toward git: file store, git-versioned store (history IS the database),
   content-addressed blob store (sha1 + dedup).
7. Modernized dated conventions under test: killed the Tag_Parser dynamic-property
   deprecation; scan.php validated against the real Tag_Wrapper::identify_tag (reflection).

FINAL STATE
-----------
38 datasets, 4481 green oracle assertions, 104 commits since entry, oracle green every
round (zero missed commits, zero oracle failures). Ground truth captured from live code;
authored expectations locked as regression baselines.
