
This folder is sparse for a reason -it makes the entry point obvious ACTUALLY naming it ENTRY-POINT is self documenting. since this is a tiny project we will build our harness and code in one place 
we should include our reference server - or  a recipe to build one - (AGAIN ONE PYTHON - SCRIPT) no shell script - no-one is impressed by it.

.json will be in the root of the folder.

	.json will have all the configuration - for all tools in this set of packaging. (a configuration registry) 
	-if a tool cannot see the registry it will throw an exeption.
	-all tools run and are designed to run in python not bash.
	-all tools need to pass a bug-report automatically (timestamped if an exception is thrown) 
		 Filename - function name - time of occurance -methods to reproduce - possible cause.


