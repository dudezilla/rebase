# MANIFEST — what is in this repository (and what gets pushed)

_Generated by `versioning/ops/generate_manifest.py`. Repo `/home/notificationsforsteven/congruency` @ branch `from-checkout-3` (`c134efd2fcae`)._

This repository (`congruency`) pushes to **`git@github.com:dudezilla/congruency.git`**. Everything listed as **pushed = yes** below is
committed and travels on `git push`. Everything under *What is NOT pushed* is gitignored.

## Root entries

| entry | type | pushed? | description |
|---|---|---|---|
| `.gitignore` | file | yes | Ignore rules — the generated/local files that are NOT committed or pushed (sqlite DBs, node_modules, dev caches, the pinned .oldcode/vendor). |
| `2a2e4f8c142eafaac061fe1cebc3c93cf27849b3/` | dir | yes | THE TOURNAMENT SUBMISSION — the commit-named entry folder. Holds the evolved tag engine, its oracle, the zero-JS showcase, the python versioning/ops apparatus, and a self-contained snapshot of the CMS. See its README.md / submission_00.txt. |
| `ENTRANTS.txt` | file | yes | Tournament entrants log — one datetime line per entry (append-only). |
| `MANIFEST.md` | file | yes | This file — generated inventory of every root entry and exactly what gets pushed to git. |
| `README.md` | file | yes | Repository readme. |
| `bin/` | dir | yes | Original congruency CMS: bootstrap/front-controller entry (Execute.php), POM init, and test harnesses. |
| `dev/` | dir | yes | Development tooling (NOT the CMS): Playwright attack loop, the 15-bug catalogue + repros, and the old-code reference. Heavy/generated parts are gitignored. |
| `doc/` | dir | yes | Original congruency documentation (Install / Version / Changes / License / NextVersion). |
| `etc/` | dir | yes | Original congruency configuration (Constants.php, Privilege.php). |
| `invocators/` | dir | yes | Original congruency tag invocators — the tag classes the parser dispatches to. |
| `lib/` | dir | yes | Original congruency library — TagLoader/parser, DAOs, persistent-object manager, form system. |
| `www/` | dir | yes | Original congruency web root (index.php, Constants.php). |

## Inside the submission folder `2a2e4f8c142eafaac061fe1cebc3c93cf27849b3/`

The tournament entry. Its own top-level:

| entry | type | pushed? | description |
|---|---|---|---|
| `LINEAGE.md` | file | yes | How the code lineage (original -> refactors -> submission) is assembled into one modular git repo. |
| `README.md` | file | yes | Submission overview, run instructions, and the seven-goal coverage map. |
| `assemble_lineage.sh` | file | yes | Apparatus: builds the modular lineage repo (original -> refactors -> submission), deterministic. |
| `bin/` | dir | yes | Snapshot of the CMS bootstrap/harness at the entry commit (so the submission evaluates in isolation). |
| `bundle_push.sh` | file | yes | Apparatus: pushes every project repo to a local git bundle (the offline stand-in remote). |
| `doc/` | dir | yes | Snapshot of the CMS documentation at the entry commit. |
| `etc/` | dir | yes | Snapshot of the CMS configuration at the entry commit. |
| `invocators/` | dir | yes | Snapshot of the CMS tag invocators at the entry commit. |
| `lib/` | dir | yes | Snapshot of the CMS library at the entry commit. |
| `package_tournament.sh` | file | yes | Apparatus: serializes the entire tournament into one self-verifying git bundle. |
| `serve_override.php` | file | yes | auto_prepend that points the harness ABS_PATH/TAGS_DIR at this submission snapshot. |
| `showcase/` | dir | yes | The redesigned, server-side, zero-JavaScript front-end rendered by the evolved engine (the default congruency home). |
| `submission_00.txt` | file | yes | Submission location relative to the main codebase + tournament protocol details. |
| `tests/` | dir | yes | The self-contained oracle and all tested data (tests/parser/: run.php + 38 datasets + the evolved engine). |
| `versioning/` | dir | yes | The python apparatus: verify_git_state, version_source (python-computed versions), checkout, the importable `ops` package (run_op/run_plan/opsrun + commit_release/refresh_bundles/verify_bundles/propagate_version/git_commit/generate_manifest/git_push), and instrumentation logs. |
| `www/` | dir | yes | Snapshot of the CMS web root at the entry commit. |

## What is NOT pushed (gitignore)

These patterns are generated/local and never committed:

```
dev/harness/php/
dev/harness/*.sqlite
dev/harness/sess_*
dev/harness/server.log
dev/tests/vendor/
dev/tests/.phpunit.cache/
dev/tests/*.sqlite
dev/coverage/_*.instrumented.php
dev/attack/node_modules/
dev/bugs/.oldcode/
dev/bugs/vendor/
*.sqlite
```

## Submodules

This repo has **no submodules of its own** (no `.gitmodules`), so its own push is not submodule-gated. The pristine original source is a submodule of a *different* repo (`congruency-bugs/vendor/congruency`), pinned at the 2006 `old-code` commit.

## Pushing to GitHub (reiterated)

`origin` = `git@github.com:dudezilla/congruency.git`. GitHub is strict about submodules: it rejects a superproject push whose
gitlink points to a commit the remote does not yet have. Push submodules first, then the
superproject, then tags:

```sh
# from a clone on a machine with network + ssh to github.com:
git -C <repo> push origin --recurse-submodules=on-demand <branch>   # or push each submodule first
git -C <repo> push origin <branch>          # e.g. from-checkout-3  (the version-2.000x line)
git -C <repo> push origin --tags            # the version-* tags
```

Use the bundled **`versioning/ops/git_push.py`** to do this robustly (retries, streams
stdout+stderr to screen and log, records exceptions) from **outside** this container.
